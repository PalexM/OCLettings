version: '3.8'

services:
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:9000
    volumes:
      - .:/app
      - static_volume:/app/static
    ports:
      - "9000:9000"
    environment:
      - PYTHONUNBUFFERED=1

volumes:
  static_volume:
